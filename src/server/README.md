# Server Layer

Esta capa alojara casos de uso backend, validaciones de dominio en servidor y acceso a repositorios.

Estructura objetivo:
- `api/` handlers de rutas.
- `services/` casos de uso.
- `repositories/` acceso a datos.
